######################
# 一番シンプルな書き方。imageをpullしてそのまま利用
######################
# docker compose -f docker-compose-1.yml up
######################
# sample-docker-comoses-foo-1  | I'm in container :)

services:
  foo:
    image: busybox
    command: echo "I'm in container :)"
