######################
# environmentを定義して出力させる
######################
# docker compose -f docker-compose-2.yml up
######################
# sample-docker-comoses-foo-1  | HOSTNAME=a962641397d5
# sample-docker-comoses-foo-1  | HOGE_ENV=test_app_test
# sample-docker-comoses-foo-1  | HOME=/root

services:
  foo:
    image: busybox
    command: env
    environment: # ★
      HOGE_ENV: test_app
